import Layout, { PageMeta } from "components/Layout";
import React from "react";

const pageMeta: PageMeta = {
    title: "Shopee Food Group Order",
    description: "Shopee Food Group Order",
};

function ShopeeFoodGroupOrder() {
    return <Layout meta={pageMeta}>Comming Soon...</Layout>;
}

export default ShopeeFoodGroupOrder;
